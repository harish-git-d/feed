spring:
  datasource:
    url: jdbc:oracle:thin:@:1521/
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20

gai:
  output-directory: /opt/scef/gai/staging/prod
  sftp:
    enabled: true
    host:
    username:
    password: ${SFTP_PASSWORD}
    remote-directory: ${GAI_SFTP_REMOTE_DIR}   # TODO: confirm with GAI team
  notification:
    enabled: true
    to: ${GAI_NOTIFY_TO}
