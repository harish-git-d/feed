spring:
  datasource:
    url: jdbc:oracle:thin:@RODSP:1521/SCEF_PROD
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20

gai:
  output-directory: /opt/scef/gai/staging/prod
  sftp:
    enabled: true
    host: clswiemap7p
    username: opiempr
    password: ${SFTP_PASSWORD}
    remote-directory: ${GAI_SFTP_REMOTE_DIR}   # TODO: confirm with GAI team
  notification:
    enabled: true
    to: ${GAI_NOTIFY_TO}
